# api/institution Documentation Rules

Update `README.md` when institution trust, authority, key, certificate, or storage behavior changes.

Update `.env.example` when environment variables change.

Update Swagger decorators when endpoints change.

Update `../../AGENTS.md` only when cross-project trust contracts or service boundaries change.
