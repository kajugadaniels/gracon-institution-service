# api/institution File Structure Rules

- Every file must start with a purpose comment.
- Every exported class, function, enum, and interface must have JSDoc.
- Every public controller/service method must have JSDoc.
- No `any`.
- Use explicit DTOs and interfaces.
- Use `const` by default.
- Delete commented-out code.
- Security-sensitive key and authority logic must explain the trust risk.
